name: testing

on:
  push:
    branches: [main]
    tags:
  pull_request:

jobs:
  main:
    uses: heyslava/workflows/.github/workflows/tox.yml@v0.2.0
    with:
      env: '["py39", "py310", "py311"]'
